# Use powershell to setup env var

In windows, we can also automatize the configuration process, for example, we can use the below power shell commands to 
set up env vars.

[Environment]::SetEnvironmentVariable('Foo','Bar')